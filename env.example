# Webhook Configuration
# Set this to your public domain (e.g., https://mydomain.com)
# This is used for Telegram webhook URLs
# Note: Webhooks will be at https://mydomain.com/regos-partner-bot/webhook/{token_prefix}
WEBHOOK_BASE_URL=https://mydomain.com/regos-partner-bot

# Database Configuration
# Path to SQLite database file (will be stored in ./data volume)
DATABASE_PATH=/app/data/telegram_bots.db

# Server Configuration
HOST=0.0.0.0
PORT=8000
APP_NAME=RegosPartnerBot

# Frontend Port (nginx)
# Port to expose the frontend on the host machine
FRONTEND_PORT=80

# CORS Origins (comma-separated)
# Add your production domain(s) here, e.g., https://your-domain.com,https://admin.your-domain.com
# Leave empty to use development defaults (localhost ports)
CORS_ORIGINS=

# Optional: PostgreSQL Configuration (if you want to use PostgreSQL instead of SQLite)
# DATABASE_URL=postgresql+asyncpg://user:password@postgres:5432/regos_bot
# DATABASE_PATH=postgresql+asyncpg://user:password@postgres:5432/regos_bot
